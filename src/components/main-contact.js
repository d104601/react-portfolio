import React, { useState } from 'react';

function form() {
    const handleInputChange = (e) => {

    };

    return (
        <div class="container">
            <form>
                
            </form>
        </div>
    );
}

export default form;